import React from 'react';
import { CustomFormikProps } from '../../../types/FormikProps';

interface Props {
    formikProps: CustomFormikProps;
}

const RegistrertBarnPart: React.FunctionComponent<Props> = (props) => <></>;

export default RegistrertBarnPart;
